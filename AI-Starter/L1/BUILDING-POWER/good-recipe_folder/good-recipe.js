// Task 1: Find oldest age using Math.max()

// Three robot objects martin, kevin, and stephanie have been provided
let oldestAge = Math.max(martin.age, kevin.age, stephanie.age);

// Task 2: Use .slice() method on alphabet string

// An alphabet variable (containing the alphabet string) has been provided
let cutFirst = alphabet.slice(10);
let cutLast = alphabet.slice(0, -3);
let cutFirstLast = alphabet.slice(5, -6);

// Task 3: Use string case methods on message variable

// A message variable has been provided
let noCaps = message.toLowerCase();
let allCaps = message.toUpperCase();
